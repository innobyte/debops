---
- name: Install requested packages
  package:
    name: '{{ item }}'
    state: 'present'
    # update_cache: True
    install_recommends: True
  with_flattened:
    - '{{ telegraf__base_packages }}'
    - '{{ telegraf__packages }}'
